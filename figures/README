Animation can be generated using the command:
mkdir frames; ffmpeg -i animation.mp4  -r 10 'frames/frame-%03d.jpg'

Followed by the command:
cd frames; convert -delay 10 -loop 0 *.jpg Shaped_Signal.gif
